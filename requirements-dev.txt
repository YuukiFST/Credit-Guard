# Testes
pytest==8.2.2
pytest-cov==5.0.0            # cobertura de código
pytest-asyncio==0.23.7       # testes async (FastAPI)
httpx==0.27.0                # TestClient do FastAPI

# Qualidade de código
ruff==0.5.0                  # linting ultra-rápido
mypy==1.10.1                 # verificação de tipos
types-PyYAML>=6.0.12         # stubs opcionais para yaml (mypy)
pre-commit==3.7.1

# Notebooks
jupyter==1.0.0
nbstripout==0.7.1            # remove outputs antes do commit
